# Processed Data Directory

Contains clean, validated, and normalized outputs produced by the ingestion and feature pipeline scripts:

- `accidents_clean.csv`: Standardized historical accident logs with `data_source` and `is_simulated` provenance flags.
- `junction_features.csv`: Calculated historical window features per junction.
